const config = {
    port: 3000,
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "hadasim",
      password: "vsxho",
      database: "hmo"
    },
    listPerPage: 20,
  };
  export default config;